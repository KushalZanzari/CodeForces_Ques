import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);                        if (scanner.hasNextInt()) {            int t = scanner.nextInt();                        while (t-- > 0) {                int n = scanner.nextInt();                int[] a = new int[n];                                                for (int i = 0; i < n; i++) {                    a[i] = scanner.nextInt();                }                                boolean possible = true;                                for (int i = 0; i < n; i++) {                    int currentIndex = i + 1;                    int value = a[i];                                        if (getOddPart(currentIndex) != getOddPart(value)) {                        possible = false;                        break;                    }                }                                if (possible) {                    System.out.println("YES");                } else {                    System.out.println("NO");                }            }        }        scanner.close();    }        private static int getOddPart(int num) {        while (num % 2 == 0) {            num /= 2;        }        return num;    }}