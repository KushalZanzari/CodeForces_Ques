import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();                while (t-- > 0) {            int a = sc.nextInt();            int b = sc.nextInt();                        int shortSide = Math.min(a, b);            int longSide = Math.max(a, b);                                    int squareSide = Math.max(shortSide * 2, longSide);                        System.out.println(squareSide * squareSide);        }    }}